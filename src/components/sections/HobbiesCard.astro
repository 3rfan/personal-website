---
import Card from '../common/Card.astro'
import { Badge } from '../ui/badge'
import { getLangFromUrl, useTranslations } from '../../i18n/utils'

const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)

const hobbies = [
  { key: 'hobbies.football', icon: '⚽' },
  { key: 'hobbies.reading', icon: '📚' },
  { key: 'hobbies.gaming', icon: '🎮' },
  { key: 'hobbies.history', icon: '📜' },
  { key: 'hobbies.languages', icon: '🌍' },
]
---

<Card colSpan="md:col-span-6 lg:col-span-2" rowSpan="md:row-span-1 lg:row-span-2" title={t('hobbies.title')}>
  <p class="mt-2 text-sm text-muted-foreground">{t('hobbies.description')}</p>
  <div class="mt-4 flex w-full flex-wrap items-center gap-2">
    {
      hobbies.map((hobby) => (
        <Badge variant="outline" className="flex items-center gap-1.5">
          <span>{hobby.icon}</span>
          <span>{t(hobby.key)}</span>
        </Badge>
      ))
    }
  </div>
</Card>
