---
import ContentCard from './ContentCard.astro'
import { Card as ShadcnCard } from '../ui/card'
import { SquareArrowOutUpRight } from 'lucide-react'

interface Props {
  title?: string
  body?: string
  colSpan?: string
  rowSpan?: string
  href?: string
  colorText?: string
}

const { title, body, colSpan, rowSpan, href, colorText } = Astro.props
---

<ShadcnCard
  className={`card-animate transfor-y-[-40%] align-start perspective-1200 group group relative col-span-1 h-auto w-full flex-none transform justify-start overflow-hidden p-4 shadow-lg transition duration-500 ease-in-out hover:border-primary ${
    colSpan || 'md:col-span-2'
  } ${rowSpan || ''} ${href ? 'cursor-pointer' : ''}`}
>
  {
    href ? (
      <a href={href} class={`block h-full w-full ${colorText || ' '}`}>
        <SquareArrowOutUpRight
          className="group-hover:text-primary position absolute right-4 "
          size={16}
        />
        <ContentCard title={title} body={body}>
          <slot />
        </ContentCard>
      </a>
    ) : (
      <ContentCard title={title} body={body}>
        <slot />
      </ContentCard>
    )
  }
</ShadcnCard>
